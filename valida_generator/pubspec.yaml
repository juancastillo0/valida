name: valida_generator
description: Code generator for valida. Create field, params and object validators from code annotations.
version: 0.0.1
homepage: https://github.com/juancastillo0/valida

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  build:
  source_gen:
  analyzer:
  valida:
    path: ../valida
    # git:
    #   url: https://github.com/juancastillo0/valida
    #   path: valida

dev_dependencies:
  build_runner:
  build_config:
  lint: ^1.0.0
  test:
# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec
